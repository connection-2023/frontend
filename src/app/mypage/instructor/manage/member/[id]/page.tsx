const page = () => {
  // const { memberInfo } = useMemberStore((state) => ({
  //   memberInfo: state.memberInfo,
  // }));

  // console.log(memberInfo);

  return <div />;
};

export default page;
